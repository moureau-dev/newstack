{
  "name": "create-newstack-app",
  "version": "0.0.1",
  "description": "Scaffold a new Newstack application",
  "type": "module",
  "bin": {
    "create-newstack-app": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --format=esm --minify --target=node20 --outfile=dist/index.js"
  },
  "dependencies": {
    "newstack": "workspace:^"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  }
}
