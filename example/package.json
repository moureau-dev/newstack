{
  "name": "@newstack/example",
  "version": "1.0.0",
  "description": "An example package to demonstrate the use of framework",
  "type": "module",
  "scripts": {
    "start": "newstack start",
    "build": "newstack build",
    "build:ssg": "newstack build && node dist/ssg.js",
    "prestart": "newstack build"
  },
  "dependencies": {
    "socket.io-client": "^4.8.1",
    "newstack": "workspace:*",
    "esbuild": "^0.25.5",
    "@swc/core": "^1.15.2"
  }
}
