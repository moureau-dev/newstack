import Newstack from "@moureau/newstack";

export class Footer extends Newstack {
  render() {
    return (
      <footer class="mt-auto px-6 py-3 border-t border-zinc-700 text-xs text-zinc-500">
        Built with Newstack
      </footer>
    );
  }
}
